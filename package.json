{
  "name": "overleaf-cc",
  "version": "2.0.0",
  "private": true,
  "description": "Overleaf Mirror - Bidirectional file sync for Claude Code",
  "scripts": {
    "build": "pnpm -r --workspace-concurrency=1 build",
    "build:parallel": "pnpm -r build",
    "build:script": "node scripts/build-all.js",
    "dev:server": "pnpm --filter @overleaf-cc/mirror-server dev",
    "dev:extension": "pnpm --filter @overleaf-cc/extension dev",
    "test": "pnpm -r test",
    "clean": "pnpm -r exec -- rm -rf dist",
    "release": "node scripts/prepare-release.js",
    "release:npm": "cd packages/mirror-server && npm publish --registry https://registry.npmjs.org/"
  },
  "devDependencies": {
    "archiver": "^6.0.2",
    "prettier": "^3.1.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
